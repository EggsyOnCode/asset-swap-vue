export interface TaskType {
  name: string;
  id: number;
  time: string;
}
export interface NftInfo {
  model: string;
  price: string;
  imgUrl: string;
  location: string;
  sellerAddress: string;
  buyerAddress: string;
  nftContractAddress: string;
}
